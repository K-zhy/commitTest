{
  "scripts": {
    "check-commit": "commitlint --from=HEAD~1",
    "commitlint": "commitlint -e"
  },
  "devDependencies": {
    "@commitlint/cli": "^15.0.2",
    "@commitlint/config-angular": "^15.0.1"
  }
}
